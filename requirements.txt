# Runtime dependency (optional enhancement): rich provides highlighted output + themes.
# The tool runs fine without it — wl auto-downgrades to plain text (see _RICH_AVAIL and --color auto in wl.py).
rich>=13
