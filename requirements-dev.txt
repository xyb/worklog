# worklog-cli dev dependencies — testing / coverage / parallel execution.
# Install: `pip install -r requirements.txt -r requirements-dev.txt`
# Or via Makefile: `make venv` (installs both sets).
#
# Conservative lower bounds: tests should run; we don't chase newest versions.

# test framework
pytest>=8.0          # pytest>=8 stable; 9 also compatible
pytest-cov>=5.0      # coverage (provides --cov-branch / --cov-fail-under)
pytest-xdist>=3.5    # parallel (-n auto, multi-core, ~3x faster)

# coverage is pytest-cov's runtime; listing it explicitly aids debugging
coverage>=7.5
