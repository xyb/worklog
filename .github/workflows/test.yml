name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.11', '3.12', '3.13']

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Sync deps
        run: uv sync --all-groups

      - name: Run tests with coverage
        run: uv run pytest --cov-report=xml --cov-report=term-missing

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          fail_ci_if_error: false

      - name: Smoke test CLI
        run: |
          export WORKLOG_DB=$(mktemp -d)/worklog.db
          uv run python wl.py init
          uv run python wl.py --version
          uv run python wl.py add "ci smoke task" -k task -p A
          uv run python wl.py log 1 "hello from CI"
          uv run python wl.py done 1
          uv run python wl.py show 1
          uv run python wl.py tree
