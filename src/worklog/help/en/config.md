---
title: config — show resolved paths & settings
category: command
see_also: admin, init
---
`wl config` prints where things live and the current settings: the DB path (and how it was
resolved), the aliases file, XDG dirs, and relevant env vars. Read-only — it doesn't create
anything. See `wl help admin`.
