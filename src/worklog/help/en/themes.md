---
title: themes — list color themes
category: command
see_also: admin
---
`wl themes` lists the available color themes with a one-line preview each. Switch with the
global `--theme {auto,dark,light,mono}` flag or `$WORKLOG_THEME`; `auto` probes the terminal
background. See `wl help admin`.
